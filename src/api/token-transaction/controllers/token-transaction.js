'use strict';

/**
 * token-transaction controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::token-transaction.token-transaction');
